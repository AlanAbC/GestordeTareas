<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.claresti.mistareas.gestordetareas">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Materias" />
        <activity android:name=".mostrarTarea" />
        <activity android:name=".agregarTarea" />
        <activity android:name=".agregar_materia" />
        <activity android:name=".horario" />
        <activity android:name=".completadas" />
        <activity android:name=".acerca" />

        <service android:name=".ServicioNotificaciones" />

        <activity android:name=".EditarMenu"></activity>
    </application>

</manifest>